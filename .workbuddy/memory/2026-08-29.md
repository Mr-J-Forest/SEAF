# 2026-08-29

## 继承 Codex 会话（01a043ac）完成 mmap 加载器切换
- Codex 在「暂停队列派发 + 编写 mmap 加载器」中途撞用量限制中断；本会话接手执行 6 步计划。
- 服务器从 westb:42977 迁移到 **westc:48312**（AutoDL 迁移，进程存活）。AGENTS.md 和 MEMORY.md 已更新地址。
- 中断后确认：seed123 与 seed3407（tuned）都正常跑完 → 正式矩阵 **9/36 完成**（full/strict/tuned 各 3 种子），队列父进程仍 SIGSTOP 暂停。
- 矩阵 `oras5_seaf_remaining_mmap_v1_matrix.json` 修正为剩余 27 任务（tuned_seed3407 移入保留列表）。
- 代码同步到服务器（source hash `69b8d55c1a`，含 data_loader 的 `preassembled_mmap_dir` 开关、build/validate 脚本）。**89 项单元测试全部通过**。
- mmap 构建：inputs_f32 12GB + climatology 完成后，写 fullfield_targets 时**磁盘 100% 打满崩溃**。根因：md0 仅 50G，中间预处理缓存（18G）+ mmap（15G）叠加超限。
- 处理：删 partial 和 .cache/preprocessed（18G）；给 build_preassembled_mmap.py 加**断点续传**（已完成数组复用、partial 重建、输出目录允许合法续传文件），已同步并重启构建。
- 待办：构建完成 → validate_preassembled_mmap.py 数值一致性校验（input/target 逐元素 + 参考预报 atol 1e-5）→ 1-epoch 冒烟对比（full/seed42 mmap vs 旧加载器首轮损失 0.780579/验证 0.932832）→ 杀掉旧暂停队列 → 启动 continuation campaign `69b8d55c1a_seaf_confirm_remaining27_mmap_v1`（--defer-post-training-evaluation --max-parallel 2）。
